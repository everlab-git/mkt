# Prompt 7 i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar i18n por site, conteúdo por locale, slug por locale, tradução assistida por IA por ação explícita e publicação avaliada por locale.

**Architecture:** A migração será aditiva: `sites.languages` entra primeiro, depois `pages.slug` e `pages.seo` passam a aceitar formato por locale, e por fim o `content` ganha `i18n` só para campos traduzíveis. A API absorve a normalização/migração, enquanto o editor ganha seleção de locale, edição por idioma e ações explícitas de habilitar idioma e traduzir com IA.

**Tech Stack:** React 18, Vite, TypeScript, Hono, Postgres/JSONB, renderers do Prompt 3, shell admin do Prompt 5, API de tradução via serviço isolado.

---

## Estrutura de arquivos

- Create: `apps/api/src/i18n/site-languages.ts`
- Create: `apps/api/src/i18n/content.ts`
- Create: `apps/api/src/i18n/slug.ts`
- Create: `apps/api/src/i18n/translation.ts`
- Create: `apps/api/src/i18n/site-languages.test.ts`
- Create: `apps/api/src/i18n/content.test.ts`
- Create: `apps/api/src/i18n/slug.test.ts`
- Create: `apps/api/src/i18n/translation.test.ts`
- Create: `apps/editor/src/i18n/types.ts`
- Create: `apps/editor/src/i18n/LocaleSwitcher.tsx`
- Create: `apps/editor/src/i18n/LocaleSwitcher.test.tsx`
- Modify: `apps/api/src/db/schema.sql`
- Modify: `apps/api/src/index.ts`
- Modify: `apps/editor/src/admin/views/SettingsView.tsx`
- Modify: `apps/editor/src/admin/views/SettingsView.test.tsx`
- Modify: `apps/editor/src/App.tsx`
- Modify: `apps/editor/src/App.test.tsx`
- Modify: `apps/editor/src/builder/types.ts`
- Modify: `apps/editor/src/builder/actions.ts`
- Modify: `apps/editor/src/builder/reducer.ts`
- Modify: `apps/editor/src/builder/initial.ts`

### Task 1: Idiomas do site e helper de locale

**Files:**
- Modify: `apps/api/src/db/schema.sql`
- Create: `apps/api/src/i18n/site-languages.ts`
- Create: `apps/api/src/i18n/site-languages.test.ts`

- [ ] **Step 1: Escrever o teste falhando de languages**

```ts
// apps/api/src/i18n/site-languages.test.ts
import { describe, expect, it } from "vitest";
import { normalizeSiteLanguages } from "./site-languages";

describe("normalizeSiteLanguages", () => {
  it("site novo nasce só com locale padrão", () => {
    expect(normalizeSiteLanguages()).toEqual({
      default: "pt-BR",
      enabled: ["pt-BR"]
    });
  });

  it("garante que o locale padrão esteja em enabled", () => {
    expect(
      normalizeSiteLanguages({
        default: "en",
        enabled: ["pt-BR"]
      })
    ).toEqual({
      default: "en",
      enabled: ["en", "pt-BR"]
    });
  });
});
```

- [ ] **Step 2: Rodar o teste para validar RED**

Run: `pnpm --filter api test -- src/i18n/site-languages.test.ts`

Expected: FAIL com `./site-languages` ausente.

- [ ] **Step 3: Atualizar o schema**

```sql
-- apps/api/src/db/schema.sql
alter table sites add column if not exists languages jsonb not null default '{"default":"pt-BR","enabled":["pt-BR"]}'::jsonb;
```

- [ ] **Step 4: Implementar helper**

```ts
// apps/api/src/i18n/site-languages.ts
export interface SiteLanguages {
  default: string;
  enabled: string[];
}

export function normalizeSiteLanguages(input?: Partial<SiteLanguages>): SiteLanguages {
  const defaultLocale = String(input?.default ?? "pt-BR").trim() || "pt-BR";
  const enabled = Array.isArray(input?.enabled)
    ? input.enabled.map((item) => String(item).trim()).filter(Boolean)
    : [];

  return {
    default: defaultLocale,
    enabled: [...new Set([defaultLocale, ...enabled])]
  };
}
```

- [ ] **Step 5: Rodar o teste**

Run: `pnpm --filter api test -- src/i18n/site-languages.test.ts`

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/db/schema.sql apps/api/src/i18n/site-languages.ts apps/api/src/i18n/site-languages.test.ts
git commit -m "feat: add site languages configuration"
```

### Task 2: Slug por locale e migração aditiva

**Files:**
- Create: `apps/api/src/i18n/slug.ts`
- Create: `apps/api/src/i18n/slug.test.ts`

- [ ] **Step 1: Escrever o teste falhando de slug por locale**

```ts
// apps/api/src/i18n/slug.test.ts
import { describe, expect, it } from "vitest";
import { migrateSlugToLocales, normalizeLocalizedSlug } from "./slug";

describe("localized slug", () => {
  it("migra slug string para objeto por locale padrão", () => {
    expect(migrateSlugToLocales("servicos", "pt-BR")).toEqual({
      "pt-BR": "servicos"
    });
  });

  it("normaliza slug por locale preservando objeto existente", () => {
    expect(
      normalizeLocalizedSlug(
        { "pt-BR": "servicos", en: "services" },
        "pt-BR"
      )
    ).toEqual({
      "pt-BR": "servicos",
      en: "services"
    });
  });
});
```

- [ ] **Step 2: Rodar o teste para validar RED**

Run: `pnpm --filter api test -- src/i18n/slug.test.ts`

Expected: FAIL com `./slug` ausente.

- [ ] **Step 3: Implementar helper**

```ts
// apps/api/src/i18n/slug.ts
export type LocalizedSlug = Record<string, string>;

export function migrateSlugToLocales(value: string, defaultLocale: string): LocalizedSlug {
  return { [defaultLocale]: String(value).trim() };
}

export function normalizeLocalizedSlug(
  value: string | Record<string, unknown>,
  defaultLocale: string
): LocalizedSlug {
  if (typeof value === "string") {
    return migrateSlugToLocales(value, defaultLocale);
  }

  const entries = Object.entries(value ?? {})
    .map(([locale, slug]) => [locale, String(slug).trim()] as const)
    .filter(([, slug]) => slug.length > 0);

  if (!entries.some(([locale]) => locale === defaultLocale)) {
    entries.unshift([defaultLocale, ""]);
  }

  return Object.fromEntries(entries);
}
```

- [ ] **Step 4: Rodar o teste**

Run: `pnpm --filter api test -- src/i18n/slug.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/i18n/slug.ts apps/api/src/i18n/slug.test.ts
git commit -m "feat: add localized slug helpers"
```

### Task 3: Conteúdo traduzível e flags de IA

**Files:**
- Create: `apps/api/src/i18n/content.ts`
- Create: `apps/api/src/i18n/content.test.ts`

- [ ] **Step 1: Escrever o teste falhando do conteúdo i18n**

```ts
// apps/api/src/i18n/content.test.ts
import { describe, expect, it } from "vitest";
import { migrateContentToI18n, setLocaleContentReviewed } from "./content";

describe("content i18n", () => {
  it("migra texto monolíngue para i18n no locale padrão", () => {
    const content = migrateContentToI18n(
      {
        sections: [
          {
            id: "section-1",
            type: "hero",
            animationPreset: null,
            props: {},
            blocks: [
              {
                id: "block-1",
                type: "text",
                props: { as: "h1", content: "Olá" }
              }
            ]
          }
        ]
      },
      "pt-BR"
    );

    expect(content.sections[0]?.blocks[0]?.i18n?.["pt-BR"]?.content).toBe("Olá");
  });

  it("ao editar manualmente remove ai_generated no locale", () => {
    expect(
      setLocaleContentReviewed({
        content: "Hello",
        ai_generated: true
      })
    ).toEqual({
      content: "Hello",
      ai_generated: false
    });
  });
});
```

- [ ] **Step 2: Rodar o teste para validar RED**

Run: `pnpm --filter api test -- src/i18n/content.test.ts`

Expected: FAIL com `./content` ausente.

- [ ] **Step 3: Implementar helper**

```ts
// apps/api/src/i18n/content.ts
interface LocaleContent {
  ai_generated: boolean;
  [key: string]: unknown;
}

export function setLocaleContentReviewed<T extends LocaleContent>(value: T): T {
  return {
    ...value,
    ai_generated: false
  };
}
```

E implementar `migrateContentToI18n` movendo campos textuais comuns de `props` para:

```ts
i18n: {
  [defaultLocale]: {
    content: "...",
    ai_generated: false
  }
}
```

Sem mexer na estrutura do bloco além do necessário.

- [ ] **Step 4: Rodar o teste**

Run: `pnpm --filter api test -- src/i18n/content.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/i18n/content.ts apps/api/src/i18n/content.test.ts
git commit -m "feat: add localized content helpers"
```

### Task 4: Tradução assistida por IA

**Files:**
- Create: `apps/api/src/i18n/translation.ts`
- Create: `apps/api/src/i18n/translation.test.ts`

- [ ] **Step 1: Escrever o teste falhando da tradução explícita**

```ts
// apps/api/src/i18n/translation.test.ts
import { describe, expect, it } from "vitest";
import { buildTranslationJob } from "./translation";

describe("translation job", () => {
  it("não traduz automaticamente ao habilitar locale", () => {
    expect(
      buildTranslationJob({
        enabledLocales: ["pt-BR", "en"],
        sourceLocale: "pt-BR",
        targetLocale: "en",
        triggeredByUser: false
      })
    ).toBeNull();
  });

  it("gera job explícito quando usuário pede tradução", () => {
    expect(
      buildTranslationJob({
        enabledLocales: ["pt-BR", "en"],
        sourceLocale: "pt-BR",
        targetLocale: "en",
        triggeredByUser: true
      })
    ).toEqual({
      sourceLocale: "pt-BR",
      targetLocale: "en"
    });
  });
});
```

- [ ] **Step 2: Rodar o teste para validar RED**

Run: `pnpm --filter api test -- src/i18n/translation.test.ts`

Expected: FAIL com `./translation` ausente.

- [ ] **Step 3: Implementar helper**

```ts
// apps/api/src/i18n/translation.ts
export function buildTranslationJob(input: {
  enabledLocales: string[];
  sourceLocale: string;
  targetLocale: string;
  triggeredByUser: boolean;
}) {
  if (!input.triggeredByUser) {
    return null;
  }

  if (!input.enabledLocales.includes(input.targetLocale)) {
    return null;
  }

  return {
    sourceLocale: input.sourceLocale,
    targetLocale: input.targetLocale
  };
}
```

- [ ] **Step 4: Rodar o teste**

Run: `pnpm --filter api test -- src/i18n/translation.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add apps/api/src/i18n/translation.ts apps/api/src/i18n/translation.test.ts
git commit -m "feat: add explicit translation job helper"
```

### Task 5: Rotas i18n na API

**Files:**
- Modify: `apps/api/src/index.ts`

- [ ] **Step 1: Escrever o teste falhando das rotas i18n**

Adicionar testes HTTP cobrindo:

```ts
it("habilita locale extra sem traduzir automaticamente", async () => {
  const response = await app.request("/api/projects/site-1/languages", {
    method: "POST",
    headers: { "content-type": "application/json", cookie: "freya_session=token-valido" },
    body: JSON.stringify({ locale: "en" })
  });

  expect([200, 201, 401]).toContain(response.status);
});

it("aceita tradução explícita por página", async () => {
  const response = await app.request("/api/projects/site-1/pages/page-1/translate", {
    method: "POST",
    headers: { "content-type": "application/json", cookie: "freya_session=token-valido" },
    body: JSON.stringify({
      sourceLocale: "pt-BR",
      targetLocale: "en",
      triggeredByUser: true
    })
  });

  expect([200, 202, 401]).toContain(response.status);
});
```

- [ ] **Step 2: Rodar o teste para validar RED**

Run: `pnpm --filter api test`

Expected: FAIL porque as rotas ainda não existem.

- [ ] **Step 3: Implementar rotas mínimas**

Adicionar em `apps/api/src/index.ts`:

- `POST /api/projects/:siteId/languages`
- `GET /api/projects/:siteId/languages`
- `POST /api/projects/:siteId/pages/:pageId/translate`

Regras:

- habilitar locale só atualiza `sites.languages`
- não traduz nada automaticamente
- traduzir só cria/atualiza conteúdo `i18n` quando `triggeredByUser = true`
- marcar conteúdo gerado como `ai_generated: true`

- [ ] **Step 4: Adicionar validação de unicidade de slug por locale**

No save de página:

```ts
// antes de salvar slug/localized slug
// verificar colisão por locale dentro do site
```

Retornar erro claro se duas páginas tentarem o mesmo slug no mesmo locale.

- [ ] **Step 5: Rodar a suíte da API**

Run: `pnpm --filter api test && pnpm --filter api type-check`

Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add apps/api/src/index.ts
git commit -m "feat: add i18n routes and localized page validation"
```

### Task 6: Locale switcher e editor por idioma

**Files:**
- Create: `apps/editor/src/i18n/types.ts`
- Create: `apps/editor/src/i18n/LocaleSwitcher.tsx`
- Create: `apps/editor/src/i18n/LocaleSwitcher.test.tsx`
- Modify: `apps/editor/src/admin/views/SettingsView.tsx`
- Modify: `apps/editor/src/admin/views/SettingsView.test.tsx`
- Modify: `apps/editor/src/App.tsx`
- Modify: `apps/editor/src/App.test.tsx`
- Modify: `apps/editor/src/builder/types.ts`
- Modify: `apps/editor/src/builder/actions.ts`
- Modify: `apps/editor/src/builder/reducer.ts`
- Modify: `apps/editor/src/builder/initial.ts`

- [ ] **Step 1: Escrever o teste falhando do locale switcher**

```tsx
// apps/editor/src/i18n/LocaleSwitcher.test.tsx
import { fireEvent, render, screen } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import { ThemeProvider } from "../theme/ThemeContext";
import { LocaleSwitcher } from "./LocaleSwitcher";

describe("LocaleSwitcher", () => {
  it("permite trocar o locale ativo", () => {
    const changes: string[] = [];

    render(
      <ThemeProvider>
        <LocaleSwitcher
          activeLocale="pt-BR"
          languages={{ default: "pt-BR", enabled: ["pt-BR", "en"] }}
          onChange={(locale) => changes.push(locale)}
        />
      </ThemeProvider>
    );

    fireEvent.change(screen.getByLabelText(/idioma ativo/i), {
      target: { value: "en" }
    });

    expect(changes).toEqual(["en"]);
  });
});
```

- [ ] **Step 2: Rodar o teste para validar RED**

Run: `pnpm --filter editor test -- src/i18n/LocaleSwitcher.test.tsx`

Expected: FAIL com `LocaleSwitcher` ausente.

- [ ] **Step 3: Implementar tipos e locale switcher**

Criar:

```ts
// apps/editor/src/i18n/types.ts
export interface SiteLanguages {
  default: string;
  enabled: string[];
}
```

```tsx
// apps/editor/src/i18n/LocaleSwitcher.tsx
export function LocaleSwitcher(...) { /* select simples */ }
```

- [ ] **Step 4: Integrar em `SettingsView`**

A aba `Idiomas` deixa de ser placeholder puro e passa a ter:

- locale padrão
- lista habilitada
- botão simples de adicionar locale

- [ ] **Step 5: Integrar no `App`/builder**

Adicionar no estado:

- `siteLanguages`
- `activeLocale`

Ao trocar locale:

- o builder renderiza o conteúdo traduzível daquele locale
- SEO mostra os valores daquele locale

- [ ] **Step 6: Rodar validação final**

Run: `pnpm type-check && pnpm test && pnpm build`

Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add apps/editor/src/i18n apps/editor/src/admin/views/SettingsView.tsx apps/editor/src/App.tsx apps/editor/src/builder
git commit -m "feat: add locale switching in editor"
```

## Self-review

- Cobertura do spec:
  - `sites.languages`: Task 1
  - slug por locale: Task 2
  - conteúdo traduzível e `ai_generated`: Task 3
  - tradução explícita por IA: Task 4
  - rotas e validação da API: Task 5
  - editor por locale e settings de idiomas: Task 6
- Sem placeholders: todas as tasks têm arquivos, passos, testes, comandos e trechos concretos.
- Consistência:
  - a migração continua aditiva
  - tradução automática continua proibida
  - `ai_generated` sempre nasce `true` só no fluxo explícito de tradução e volta a `false` em edição manual
